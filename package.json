{
  "name": "draugar",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "postinstall": "pnpm --filter @draugar/shared build"
  },
  "packageManager": "pnpm@9.15.0",
  "pnpm": {
    "overrides": {
      "socket.io-parser": ">=4.2.6",
      "esbuild": ">=0.25.0"
    }
  }
}
