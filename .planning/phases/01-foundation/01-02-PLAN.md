---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [packages/mobile/package.json, packages/mobile/tsconfig.json, packages/mobile/app.json, packages/mobile/App.tsx, packages/mobile/babel.config.js]
autonomous: true
---

<objective>
Scaffold React Native Expo project in monorepo.

Purpose: Create the mobile app foundation using Expo for faster development and easier builds. Link to shared types for type safety.
Output: Working Expo app that imports from @draugar/shared and runs on iOS/Android simulators.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Expo project</name>
  <files>packages/mobile/package.json, packages/mobile/app.json, packages/mobile/babel.config.js</files>
  <action>
    1. cd packages/mobile
    2. Run: npx create-expo-app@latest . --template blank-typescript
       - This creates package.json, app.json, tsconfig.json, App.tsx, babel.config.js
    3. Update package.json:
       - name: "@draugar/mobile"
       - Add dependency: "@draugar/shared": "workspace:*"
    4. Run pnpm install from root to link workspace dependency
  </action>
  <verify>ls packages/mobile shows App.tsx, package.json, app.json exist</verify>
  <done>Expo project initialized with TypeScript template</done>
</task>

<task type="auto">
  <name>Task 2: Configure TypeScript and verify shared types</name>
  <files>packages/mobile/tsconfig.json, packages/mobile/App.tsx</files>
  <action>
    1. Update packages/mobile/tsconfig.json to extend root config if needed, ensure:
       - strict: true
       - Module resolution works with workspace packages
    2. Update App.tsx to:
       - Import { User, Location } from '@draugar/shared'
       - Display simple "Draugar" text with StatusBar
       - Add a comment showing types are accessible (for verification)
    3. Ensure TypeScript compiles without errors
  </action>
  <verify>cd packages/mobile && npx tsc --noEmit passes (no type errors)</verify>
  <done>Mobile app imports shared types successfully, TypeScript passes</done>
</task>

<task type="auto">
  <name>Task 3: Verify app runs</name>
  <files>packages/mobile/App.tsx</files>
  <action>
    1. From packages/mobile, run: npx expo start --web (fastest to verify)
    2. Confirm app launches without runtime errors
    3. The app should show "Draugar" text

    Note: Full iOS/Android simulator testing is optional for foundation - web mode confirms the build works.
  </action>
  <verify>npx expo start --web launches, browser shows app without errors</verify>
  <done>Expo app runs and displays UI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] packages/mobile/ has valid Expo project structure
- [ ] @draugar/shared is listed as workspace dependency
- [ ] TypeScript compiles without errors (npx tsc --noEmit)
- [ ] App.tsx imports from @draugar/shared successfully
- [ ] `npx expo start --web` launches app
</verification>

<success_criteria>

- All tasks completed
- Expo project scaffolded in monorepo
- Shared types accessible from mobile package
- App runs in at least web mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
