# Server configuration
# Note: Port 3001 may conflict with other services, 3002 is an alternative
PORT=3001

# Database configuration
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/draugar

# JWT configuration
# Secret key for signing JWT tokens (generate a secure random string for production)
JWT_SECRET=your-secure-jwt-secret-here
